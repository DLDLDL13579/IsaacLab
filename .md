# Isaac Lab的最终导出文件：policy.onnx和policy.pt

# 系统兼容性说明：
失败的原因是驱动太新，虽然兼容性检测似乎可行，但是当驱动改为580以后就能成功运行。

# USD文件
开源项目的USD文件包含的功能不一定全面，可能还需添加物理参数（质量/摩擦）、驱动参数（电机）、传感器读取逻辑、控制接口、任务/奖励/观测等,最好不要随便看，就根据其说明文档进行开发，即可

# 目前由于不是首次运行Isaac Sim，没有遇到“激活lisence”/“登录nvidia账号”的问题，
也没有遇到注册“nvidia ngc”账号，获取API key拉取镜像的问题

# 其他项目使用Isaac Lab训练直接挂载即可，目录在/workspace/xxx，此时所有的数据都直接显示在宿主机上，不会存在删除容器丢失数据的问题（比如rl_training）。
# 下面的这两个目录均在容器内部使用，一旦删除容器这些数据全部会丢失；另外，如果长期没有启动容器，可能会自行删除（也可能是记错了）。

isaac lab 的训练输出主要在 /workspace/isaaclab/logs里面，包含参数：agent.yaml、env.yaml，events.out.xxx（tensorboard 用于查看日志信息），model_xx.pt（保存模型参数）；当执行导出指令后（先训练才能导出），会在/logs里面创建/logs/exported目录，里面包含policy.onnx和policy.pt文件。
还有一些输出信息在 /workspace/isaaclab/outputs里面，包含config.yaml、hydra.yaml、overrides.yaml、hydra.log。

# 过程：让可视化界面顺利在docker中显示
确保X11授权成功
```bash
export DISPLAY=:0
export XAUTHORITY=/home/gkfd/.Xauthority
cp /run/user/1000/gdm/Xauthority ~/.Xauthority 2>/dev/null || true

1.xhost +local:docker 
2.docker exec -it NAME bash 进入docker
echo $DISPLAY 验证，确保输出为“:0”
```

# 最好在docker启动时删除掉“--rm”这一参数（停止即删除），避免容器停止后容器被删除里面的数据全部清空（未挂载的工作目录）
# isaac sim实际上并不需要单独启动，启动Lab，并在Lab中直接启动Sim即可。Sim中缺少很多创建软件包，导致指令不能用（比如tree、cd···）

# 或者先xhost +local:docker；再进入/启动;id 1234:1234是特定用户Id，不能修改
```bash
xhost +local:
docker run --name isaac-sim \
    --entrypoint bash -it \
    --gpus all \
    -e "ACCEPT_EULA=Y" \
    -e "PRIVACY_CONSENT=Y" \
    --network=host \
    -e DISPLAY \
    -v $HOME/.Xauthority:/isaac-sim/.Xauthority \
    -v ~/docker/isaac-sim/cache/main:/isaac-sim/.cache:rw \
    -v ~/docker/isaac-sim/cache/computecache:/isaac-sim/.nv/ComputeCache:rw \
    -v ~/docker/isaac-sim/logs:/isaac-sim/.nvidia-omniverse/logs:rw \
    -v ~/docker/isaac-sim/config:/isaac-sim/.nvidia-omniverse/config:rw \
    -v ~/docker/isaac-sim/data:/isaac-sim/.local/share/ov/data:rw \
    -v ~/docker/isaac-sim/pkg:/isaac-sim/.local/share/ov/pkg:rw \
    -u 1234:1234 \
    nvcr.io/nvidia/isaac-sim:5.1.0
```

# Isaac Lab docker启动：
注意：训练推荐使用 --headless 模式，图形界面可能不稳定
如果遇到 X11 授权问题（Authorization required, but no authorization protocol specified）

```bash
# 1. 首先设置正确的环境变量
export DISPLAY=:0
export XAUTHORITY=/home/gkfd/.Xauthority
cp /run/user/1000/gdm/Xauthority ~/.Xauthority 2>/dev/null || true

# 2. 允许本地连接（不要用 xhost +local:docker，用 xhost +local:）
xhost +local:

# docker exec -it isaac-lab bash 
# ./_isaac_sim/runapp.sh

# 3. 启动容器
docker run --name isaac-lab --entrypoint bash -it --gpus all -e "ACCEPT_EULA=Y" --network=host \
   -e "PRIVACY_CONSENT=Y" \
   -e DISPLAY \
   -v $HOME/.Xauthority:/root/.Xauthority \
   -v ~/rl_training:/workspace/rl_training\
   -v ~/docker/isaac-sim/cache/kit:/isaac-sim/kit/cache:rw \
   -v ~/docker/isaac-sim/cache/ov:/root/.cache/ov:rw \
   -v ~/docker/isaac-sim/cache/pip:/root/.cache/pip:rw \
   -v ~/docker/isaac-sim/cache/glcache:/root/.cache/nvidia/GLCache:rw \
   -v ~/docker/isaac-sim/cache/computecache:/root/.nv/ComputeCache:rw \
   -v ~/docker/isaac-sim/logs:/root/.nvidia-omniverse/logs:rw \
   -v ~/docker/isaac-sim/data:/root/.local/share/ov/data:rw \
   -v ~/docker/isaac-sim/documents:/root/Documents:rw \
   nvcr.io/nvidia/isaac-lab:2.3.2 

./_isaac_sim/runapp.sh
```

# 5. 训练命令（推荐使用 --headless 模式，图形界面可能不稳定）：
# ./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/train.py --task Isaac-Velocity-Flat-H1-v0 --headless

# 日志文件
包括日志文件和其他文件容器内部产生的文件等大都在“/home/gkfd/docker/isaac-sim”当中


流程：
- 将X.usd文件复制到容器中或确保容器可以访问该文件

- 在Isaac Sim中查看检查X30.usd文件的功能和结构

- 参考H1机器人的配置文件结构，为X30创建类似的配置文件

- 创建X30的任务配置文件，包括环境设置、奖励函数等

- 创建X30的RL代理配置文件

- 使用训练脚本训练X30的控制策略

- 使用导出脚本导出训练好的策略

- 验证导出的策略是否正常工作